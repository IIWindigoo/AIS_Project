# СпортКлуб - Система управления фитнес-центром

Полнофункциональная веб-платформа для управления фитнес-клубом с системой бронирования тренировок, абонементами и аналитикой нагрузки тренеров.

## 🚀 Технологии

### Backend
- **FastAPI** - современный асинхронный Python веб-фреймворк
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **Alembic** - управление миграциями базы данных
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация через HTTP-only cookies
- **Pydantic** - валидация данных и схемы

### Frontend
- **Vanilla JavaScript (ES6+)** - чистый JavaScript без фреймворков
- **HTML5 & CSS3** - современная адаптивная верстка
- **Fetch API** - взаимодействие с REST API
- **Client-side routing** - SPA навигация
- **CSS Grid & Flexbox** - современная компоновка

## 📁 Структура проекта

```
project/
├── app/
│   ├── frontend/           # Фронтенд приложение (SPA)
│   │   ├── index.html
│   │   ├── css/
│   │   │   └── styles.css  # Все стили приложения
│   │   └── js/
│   │       ├── api.js      # API клиент
│   │       ├── auth.js     # Управление аутентификацией
│   │       ├── router.js   # Client-side routing
│   │       ├── utils.js    # Вспомогательные функции
│   │       ├── components.js # UI компоненты
│   │       ├── pages.js    # Страницы приложения
│   │       └── main.js     # Точка входа
│   ├── users/              # Модуль пользователей и ролей
│   ├── trainings/          # Модуль тренировок
│   ├── bookings/           # Модуль бронирований
│   ├── rooms/              # Модуль помещений
│   ├── subscriptions/      # Модуль абонементов
│   ├── memberships/        # Модуль управления членством
│   ├── dao/                # Data Access Objects (базовые классы)
│   ├── dependencies/       # FastAPI зависимости
│   ├── migrations/         # Миграции БД (Alembic)
│   ├── config.py           # Конфигурация приложения
│   ├── exceptions.py       # Кастомные исключения
│   └── main.py             # Точка входа приложения
├── tests/                  # Тесты (pytest)
├── requirements.txt        # Python зависимости
├── .env                    # Файла окружения
└── README.md
```

## 🎯 Функциональность

### Роли пользователей

#### 👤 Клиент (Client)
- Просмотр доступных тренировок и расписания
- Запись на групповые тренировки
- Управление своими бронированиями (просмотр, отмена)
- Просмотр и покупка абонементов
- Подача заявок на абонементы
- Просмотр информации о своем активном абонементе
- Личный кабинет с информацией о членстве

#### 💪 Тренер (Trainer)
- Все функции клиента
- Создание и планирование тренировок
- Редактирование своих тренировок (время, описание, помещение)
- Удаление своих тренировок
- Просмотр списка участников каждой тренировки
- **Статистика нагрузки**:
  - Общее количество тренировок
  - Общее количество клиентов
  - Среднее количество участников на тренировку
  - График распределения тренировок по дням недели
  - График распределения клиентов по дням недели
- Личный кабинет с аналитикой

#### 👑 Администратор (Admin)
- Все функции тренера
- Управление всеми тренировками (создание, редактирование, удаление)
- Управление абонементами (создание, редактирование, удаление)
- **Панель администратора**:
  - Просмотр всех пользователей системы (с пагинацией)
  - Управление заявками на абонементы (одобрение/отклонение)
  - Просмотр всех активных абонементов пользователей
  - Пагинация по 5 элементов на страницу
  - Раскрывающиеся карточки заявок

### Ключевые возможности

#### Аутентификация и безопасность
- ✅ Регистрация новых пользователей
- 🔐 Безопасная аутентификация через JWT в HTTP-only cookies
- 🔑 Система ролей (Client, Trainer, Admin)
- 🚪 Выход из системы с очисткой cookies

#### Управление тренировками
- 📅 CRUD операции для тренировок
- ⏰ Указание даты, времени начала и окончания
- 🏠 Привязка к конкретному помещению
- 👥 Просмотр списка записавшихся клиентов
- 📝 Описание тренировки

#### Система бронирования
- 🎫 Запись клиентов на тренировки
- ❌ Отмена записи
- 📊 Отслеживание количества участников
- 🔔 Визуальная индикация статуса записи

#### Абонементы и членство
- 💳 Создание различных типов абонементов
- 💰 Указание цены и срока действия (в днях)
- 📋 Система заявок на абонементы
- ✅ Процесс одобрения администратором
- 📅 Автоматический расчет даты окончания абонемента
- 👀 Просмотр активных абонементов пользователей

#### Аналитика для тренеров
- 📊 Интерактивные графики нагрузки
- 📈 Статистические показатели (всего тренировок, клиентов, среднее)
- 📉 Распределение по дням недели
- 🎨 Визуализация с градиентными столбчатыми диаграммами

#### UI/UX
- 📱 Полностью адаптивный дизайн (мобильные устройства, планшеты, десктоп)
- 🎨 Современный дизайн с градиентами и анимациями
- 🔔 Система уведомлений (успех, ошибка, предупреждение)
- 💬 Модальные окна для подтверждения действий
- 📄 Пагинация с интуитивным управлением
- 🎭 Раскрывающиеся элементы (accordion) для экономии места

## 🛠 Установка и запуск

### Предварительные требования

- Python 3.10+
- PostgreSQL 13+
- pip (менеджер пакетов Python)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd project
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# macOS/Linux
source .venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE sportclub_db;
```

Создайте файл `.env` в корне проекта:

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASS=your_password
DB_NAME=sportclub_db

# Security
SECRET_KEY=your_very_secret_key_min_32_characters_long
ALGORITHM=HS256
```

### 5. Применение миграций

```bash
alembic upgrade head
```

### 6. Запуск приложения

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: **http://localhost:8000**

### 7. Создание первого администратора

После запуска приложения:
1. Перейдите на http://localhost:8000
2. Зарегистрируйтесь как новый пользователь
3. В базе данных вручную измените роль пользователя на `admin`:

```sql
UPDATE users SET role_id = (SELECT id FROM roles WHERE name = 'admin')
WHERE email = 'your_email@example.com';
```

## 📚 API Документация

FastAPI автоматически генерирует интерактивную документацию API:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### Аутентификация
- `POST /users/register/` - Регистрация нового пользователя
- `POST /users/login/` - Вход в систему (устанавливает cookies)
- `POST /users/logout/` - Выход из системы (удаляет cookies)
- `GET /users/me/` - Получить данные текущего пользователя
- `GET /users/` - Получить всех пользователей (только для админа)

#### Тренировки
- `GET /trainings/` - Получить все тренировки
- `POST /trainings/` - Создать тренировку (тренер/админ)
- `PATCH /trainings/{id}/` - Обновить тренировку (тренер/админ)
- `DELETE /trainings/{id}/` - Удалить тренировку (тренер/админ)
- `GET /trainings/my/` - Получить свои тренировки с участниками (тренер)

#### Бронирования
- `POST /bookings/` - Записаться на тренировку (клиент)
- `GET /bookings/` - Получить свои записи (клиент)
- `DELETE /bookings/` - Отменить запись (клиент)

#### Абонементы
- `GET /subscriptions/` - Получить все абонементы
- `POST /subscriptions/` - Создать абонемент (админ)
- `PATCH /subscriptions/{id}/` - Обновить абонемент (админ)
- `DELETE /subscriptions/{id}/` - Удалить абонемент (админ)

#### Членство
- `POST /memberships/request/` - Подать заявку на абонемент (клиент)
- `GET /memberships/request/` - Получить все заявки (админ)
- `PATCH /memberships/request/{id}/` - Обновить статус заявки (админ)
- `GET /memberships/my/` - Получить свой активный абонемент (клиент)
- `GET /memberships/all/` - Получить все абонементы (админ)

#### Помещения
- `GET /rooms/` - Получить все помещения
- `POST /rooms/` - Создать помещение (админ)
- `PATCH /rooms/{id}/` - Обновить помещение (админ)
- `DELETE /rooms/{id}/` - Удалить помещение (админ)

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=app

# Запуск конкретного модуля
pytest tests/test_users.py
```

## 🎨 Frontend

Фронтенд - это Single Page Application (SPA) на чистом JavaScript, которое:

### Архитектура
- **Модульная структура** - разделение на логические модули (api, auth, router, pages, components)
- **Client-side routing** - навигация без перезагрузки страницы
- **REST API интеграция** - через Fetch API
- **Cookie-based auth** - HTTP-only cookies для безопасности
- **Reactive updates** - динамическое обновление UI

### Особенности дизайна
- **Современный градиентный дизайн** в фиолетово-синих тонах
- **Анимации** - плавные переходы и эффекты наведения
- **Графики** - визуализация данных с помощью CSS
- **Адаптивность** - оптимизация для всех размеров экранов
- **Доступность** - семантическая разметка

### Страницы
1. **Главная** - Hero-секция, услуги, преимущества, CTA
2. **Тренировки** - список с фильтрацией, создание/редактирование
3. **Мои записи** - управление бронированиями (для клиентов)
4. **Мои тренировки** - список с участниками (для тренеров)
5. **Абонементы** - покупка и управление
6. **Профиль** - личная информация, абонемент (клиент) или статистика (тренер)
7. **Админ-панель** - управление пользователями, заявками, абонементами

## 🔒 Безопасность

### Меры безопасности
- **Bcrypt** - хеширование паролей с солью
- **HTTP-only cookies** - токены недоступны для JavaScript
- **JWT** - подписанные токены с временем жизни
- **CORS** - настроенная политика доступа
- **Role-based access control** - проверка прав на уровне роутов
- **Pydantic validation** - валидация всех входных данных
- **SQL injection protection** - через SQLAlchemy ORM
- **XSS protection** - экранирование пользовательского ввода

### Рекомендации для production
1. Используйте `secure=True` для cookies (требует HTTPS)
2. Настройте строгую CORS политику
3. Используйте переменные окружения для секретов
4. Регулярно обновляйте зависимости
5. Включите rate limiting
6. Настройте мониторинг и логирование

## 📝 Разработка

### Добавление новой миграции

```bash
# Создать миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить миграцию
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

### Структура модуля

Каждый модуль следует паттерну:
- `models.py` - SQLAlchemy модели (таблицы БД)
- `schemas.py` - Pydantic схемы (валидация, сериализация)
- `dao.py` - Data Access Objects (работа с БД)
- `router.py` - FastAPI роуты (эндпоинты API)

### Добавление новой роли

1. Создайте миграцию для добавления роли в таблицу `roles`
2. Обновите зависимости в `app/dependencies/auth_dep.py`
3. Добавьте соответствующие проверки в роутеры
4. Обновите frontend для отображения функций новой роли

## 🚀 Деплой

### Production настройки

1. **Установите production зависимости**:
```bash
pip install uvicorn[standard] gunicorn
```

2. **Настройте переменные окружения** (используйте безопасное хранилище секретов)

3. **Настройте Nginx** как reverse proxy:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /static {
        alias /path/to/app/frontend;
    }
}
```

4. **Включите HTTPS** (Let's Encrypt):
```bash
certbot --nginx -d your-domain.com
```

5. **Запустите приложение**:
```bash
gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

6. **Настройте systemd service** для автозапуска:
```ini
[Unit]
Description=SportClub FastAPI Application
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/path/to/project
Environment="PATH=/path/to/project/.venv/bin"
ExecStart=/path/to/project/.venv/bin/gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000

[Install]
WantedBy=multi-user.target
```

### Мониторинг

Рекомендуемые инструменты:
- **Sentry** - отслеживание ошибок
- **Prometheus + Grafana** - метрики и визуализация
- **ELK Stack** - централизованное логирование

## 📊 Нагрузочное тестирование

Приложение включает полный набор инструментов для нагрузочного тестирования с использованием **Locust**.

### Быстрый старт

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите приложение
./start.sh

# 3. Запустите тесты (в другом терминале)
./test_frontend.sh
```

### Файлы для тестирования

- **locust_test.py** - сценарии нагрузочного тестирования
- **start.sh** - запуск приложения через gunicorn с множественными воркерами
- **test_frontend.sh** - автоматизированный запуск всех фаз тестирования
- **reports/** - директория с HTML отчетами тестов

### Фазы тестирования

1. **Фаза 1**: Только GET запросы (10 пользователей)
2. **Фаза 2**: GET + PUT запросы (15 пользователей)
3. **Фаза 3**: Все типы запросов (20 пользователей)
4. **Фаза 4**: Стресс-тест для поиска предела (50+ пользователей)

### Документация

- [QUICKSTART.md](QUICKSTART.md) - быстрый старт по нагрузочному тестированию
- [LOAD_TESTING.md](LOAD_TESTING.md) - подробная инструкция с примерами и анализом

### Ручной запуск тестов

```bash
# С веб-интерфейсом
locust -f locust_test.py --host=http://localhost:8000

# Headless режим
locust -f locust_test.py --host=http://localhost:8000 --users 20 --spawn-rate 4 --run-time 5m --html reports/test.html --headless
```

Откройте Web UI: http://localhost:8089

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 👥 Авторы

- Разработчик - [Ваше имя]

## 📞 Контакты

- Email: your.email@example.com
- GitHub: [@yourusername](https://github.com/yourusername)

---

**СпортКлуб** - Современная система управления фитнес-центром 💪
